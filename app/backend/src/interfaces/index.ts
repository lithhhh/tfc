import { ILogin, IJwtPayload, Authorization, IId } from './login';
import { IMatch, IMatchWithScore } from './match';

export {
  ILogin,
  IJwtPayload,
  Authorization,
  IId,
  IMatch,
  IMatchWithScore,
};
